# Dépendances
node_modules/
.pnpm-store/

# Sorties de build régénérables
_build/sortie/
_build/html/
*.tsbuildinfo

# Secrets — aucun n'a de raison d'exister ici, la règle est préventive
.env
.env.*
!.env.example

# Système
.DS_Store
Thumbs.db
desktop.ini

# Éditeurs
.vscode/
.idea/
*.swp

# Temporaires
*.log
tmp/

# Polices recuperees par _build/recuperer-polices.mjs — regenerables
# (on les versionne malgre tout : un depot qui ne rend pas ses propres pages
#  sans reseau ne serait pas un systeme de design autonome)
